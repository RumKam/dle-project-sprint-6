# Проект: Pretrain и SFT для русскоязычной LLM

## Описание задачи

В рамках проекта выполнена реализация двух ключевых этапов создания языковой модели: **предобучение (pretrain)** на корпусе русской литературы и **дообучение с учителем (SFT)** на инструктивном датасете.  
Цель — отработать на практике полный цикл обучения LLM: от сбора и предобработки данных до тонкой настройки модели и оценки качества генерации.

## Цели проекта

- **Pretrain**  
  Обучить модель с архитектурой decoder-only (≈150M параметров) продолжать фразы в стиле русской литературы.  
  Использован корпус произведений из репозитория `RussianNovels`.  

- **Post-train SFT**  
  Адаптировать модель `Qwen2.5-0.5B` к диалоговому формату и инструкциям на русском языке.  
  Использован датасет `d0rj/alpaca-cleaned-ru`.  

## План проекта

### Этап 1. Pretrain

1. Скачаны все тексты из репозитория `RussianNovels/corpus` и объединены в единый датасет
2. Проведён препроцессинг: удалены дубликаты, предложения с не-кириллицей, нормализована пунктуация
3. Текст разбит на чанки длиной 512 токенов с добавлением `<bos>`/`<eos>`
4. Обучен BPE-токенизатор на 3000 токенов
5. Данные токенизированы и представлены в виде `datasets.Dataset`
6. Инициализирована модель decoder-only (hidden_size=1024, intermediate=1536, layers=16, heads=16, kv_heads=8)
7. Обучение запущено с помощью `Trainer` (batch_size=128, weight_decay=0.01, learning_rate=5e-4)
8. Добавлены коллбэки для валидации на `test_prompts`
9. Сгенерированы ответы на тестовые промпты

### Этап 2. Post-train SFT

1. Загружен датасет `d0rj/alpaca-cleaned-ru`
2. Датасет преобразован в формат диалогов: `system` (если есть), `user`, `assistant`
3. Загружена базовая модель `Qwen2.5-0.5B`
4. Настроен `SFTTrainer` (batch_size=1, learning_rate=2e-5, max_length=1024)
5. Модель дообучена на инструктивном датасете (3 эпохи)
6. Выполнена генерация ответов на контрольные вопросы

## Результаты

### Pretrain

- **Промпт:** *Все мысли, которые имеют огромные последствия*  
  **Продолжение:** ответ


### SFT ответы на вопросы после дообучения

- **Вопрос:** *сколько планет в нашей солнечной системе?*  
  **Ответ:** ответ


### Сравнение качества до и после SFT

- **Базовая модель (без SFT):**  
  - Осмысленность ответов: Низкая
  - Грамотность: Неудовлетворительная  
  - Следование инструкции: Отсутствует  

- **Модель после SFT:**  
  - Осмысленность ответов: Высокая  
  - Грамотность: Хорошая  
  - Следование инструкции: Соблюдается  


## Запуск проекта

1. Установить зависимости из `requirements.txt`
2. Запустить Jupyter Notebook `solution.ipynb`
3. Следовать шагам, описанным в ноутбуке

## Выводы

- Разработанный пайплайн позволяет обучить небольшую модель генерации русскоязычных текстов с нуля, а затем адаптировать её к диалоговому формату.
- Продемонстрировано, что даже после pretrain на ограниченном корпусе модель способна улавливать стилистику и грамматику, а SFT радикально улучшает осмысленность и полезность ответов.